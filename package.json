{
  "name": "trajectory-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:main": "cd apps/main && npm run dev",
    "dev:lane": "cd apps/lane-game && npm run dev",
    "build:main": "cd apps/main && npm run build",
    "build:lane": "cd apps/lane-game && npm run build",
    "build": "npm run build:main && npm run build:lane",
    "lint": "cd apps/main && npm run lint && cd ../lane-game && npm run lint",
    "typecheck": "cd apps/main && npm run typecheck && cd ../lane-game && npm run typecheck",
    "install:all": "npm install && cd apps/main && npm install && cd ../lane-game && npm install"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20.19.11"
  },
  "packageManager": "npm@10.0.0"
}
