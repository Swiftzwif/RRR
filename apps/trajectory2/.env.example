# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Square Payment Configuration
SQUARE_ACCESS_TOKEN=your_square_access_token
SQUARE_LOCATION_ID=your_square_location_id
SQUARE_ENVIRONMENT=sandbox # or production
SQUARE_WEBHOOK_SIGNATURE_KEY=your_square_webhook_signature

# Email Configuration (Resend)
RESEND_API_KEY=your_resend_api_key

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
CONTACT_EMAIL=your_contact_email@example.com

# Google OAuth Configuration (Configure in Supabase Dashboard)
# 1. Go to Supabase Dashboard > Authentication > Providers
# 2. Enable Google provider
# 3. Add your Google OAuth credentials:
#    - Client ID from Google Cloud Console
#    - Client Secret from Google Cloud Console
# 4. Set redirect URL in Google Cloud Console to:
#    https://your-project-ref.supabase.co/auth/v1/callback
#
# Google Cloud Console Setup:
# 1. Create a new project or select existing at https://console.cloud.google.com
# 2. Enable Google+ API
# 3. Create OAuth 2.0 credentials
# 4. Add authorized redirect URIs:
#    - https://your-project-ref.supabase.co/auth/v1/callback (production)
#    - http://localhost:3000/api/auth/callback (development)

# Optional: Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=